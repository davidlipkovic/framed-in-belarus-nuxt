{
  "name": "nuxt2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "export NODE_ENV=development && export NUXT_ENV_PORT=3000 && nuxt",
    "dev_server": "export NODE_ENV=development && export NUXT_ENV_PORT=8080 && nuxt",
    "build_for_local": "export NODE_ENV=production && export NUXT_ENV_PORT=3000 && nuxt build",
    "build_for_server": "export NODE_ENV=production && export NUXT_ENV_PORT=8080 && nuxt build",
    "start_local": "export NODE_ENV=production && export NUXT_ENV_PORT=3000 && nuxt start",
    "start": "export NODE_ENV=production && export NUXT_ENV_PORT=8080 && nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/i18n": "^7.3.0",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7",
    "vue-final-modal": "^2.4.3"
  },
  "devDependencies": {
    "@nuxt/image": "^0.7.1",
    "sass": "^1.38.0",
    "sass-loader": "^10.2.0"
  }
}
